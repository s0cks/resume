#import "../../util.typ": skill
#metadata("Docker")<skills>
#metadata("Chromium")<skills>
#metadata("CI/CD")
A #skill[Docker] image to run a #skill[Chromium] instance and take a screenshot of a configurable #skill[SPA] to produce beautiful data presentation cards using web technologies.
