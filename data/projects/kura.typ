#import "../../util.typ": skill
#metadata("C++") <skills>
#metadata("LLVM") <skills>
#metadata("CMake") <skills>
#metadata("OpenGL") <skills>
#metadata("Clang") <skills>
#metadata("Vulkan") <skills>
#metadata("Shaders") <skills>
#metadata("Compilers") <skills>
An experimental #skill[LLVM] compiler and runtime for a @DSL:a designed to build highly optimized declarative and reactive UIs with #skill[OpenGL], #skill[Vulkan] and other graphics backends.
