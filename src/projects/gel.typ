#import "../util.typ": skill
#metadata("Clojure") <skills>
#metadata("LLVM") <skills>
#metadata("C++") <skills>
#metadata("JIT") <skills>
#metadata("Compiler") <skills>
#metadata("GitHub Actions") <skills>
#metadata("Garbage Collectors") <skills>
#metadata("Cheney's Algorithm") <skills>
#metadata("Parallel Scavenge Algorithm") <skills>
#metadata("Dart") <skills>
#metadata("V8") <skills>
#metadata("Nodejs") <skills>
#metadata("Javascript") <skills>
A purely functional programming language inspired by #skill[Clojure], with #skill[LLVM] based just-in-time (#skill[JIT]) compilation
and parallel garbage collection (#skill[GC]) based on Cheney's Algorithm and the Parallel Scavenge algorithm
found in #skill[Dart] and #skill[V8].
